import { Oswald } from "next/font/google";

/** Light weight (300) for brand name – slightly thinner than Impact, same condensed feel */
export const fontBrand = Oswald({
  weight: "300",
  subsets: ["latin"],
  display: "swap",
});
